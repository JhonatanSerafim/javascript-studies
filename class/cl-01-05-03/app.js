// Retornando valores e exercícios

// E quando queremos retornar valores numa função

// Quando queremos usar o resultado de uma operação para outra utilidade que não seja apresentar no console.log



const double = function (number) {
    const doubleResult = number * 2
    console.log(doubleResult)
}

double(3)

```txt

```

// Nesse código exibimos o valor 6 na saida porem não podemos fazer nada com ele, 
// pois criamos a variavel doubleResult dentro do escopo da função double
// 